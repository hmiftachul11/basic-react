// a. Bagaimana Anda menggunakan fungsi map untuk menggandakan setiap elemen dalam array?
const arrayDataNumerik = [2,4,6,8,10];
const multipleArray = arrayDataNumerik.map((arrayItem) => {
    return arrayItem * 2;
})

console.log(multipleArray);

// b. Buatlah contoh penggunaan map untuk mengubah setiap elemen dalam array string menjadi uppercase.

